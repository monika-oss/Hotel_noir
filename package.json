{
  "name": "hotel-noir",
  "version": "1.0.0",
  "description": "Hotel Noir Full Stack Deployment",
  "scripts": {
    "install": "cd server && npm install && cd ../client && npm install",
    "build": "cd client && npm run build",
    "start": "cd server && npm start"
  }
}
